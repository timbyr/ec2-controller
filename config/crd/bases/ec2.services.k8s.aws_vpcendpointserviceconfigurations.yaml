
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: vpcendpointserviceconfigurations.ec2.services.k8s.aws
spec:
  group: ec2.services.k8s.aws
  names:
    kind: VPCEndpointServiceConfiguration
    listKind: VPCEndpointServiceConfigurationList
    plural: vpcendpointserviceconfigurations
    singular: vpcendpointserviceconfiguration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VPCEndpointServiceConfiguration is the Schema for the VPCEndpointServiceConfigurations
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VpcEndpointServiceConfigurationSpec defines the desired state
              of VpcEndpointServiceConfiguration.
            properties:
              acceptanceRequired:
                description: Indicates whether requests from service consumers to
                  create an endpoint to your service must be accepted. To accept a
                  request, use AcceptVpcEndpointConnections.
                type: boolean
              allowedPrincipals:
                description: The Amazon Resource Names (ARN) of one or more principals.
                  Permissions are granted to the principals in this list. To grant
                  permissions to all principals, specify an asterisk (*).
                items:
                  type: string
                type: array
              clientToken:
                description: Unique, case-sensitive identifier that you provide to
                  ensure the idempotency of the request. For more information, see
                  How to ensure idempotency (https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Run_Instance_Idempotency.html).
                type: string
              gatewayLoadBalancerARNs:
                description: The Amazon Resource Names (ARNs) of one or more Gateway
                  Load Balancers.
                items:
                  type: string
                type: array
              networkLoadBalancerARNs:
                description: The Amazon Resource Names (ARNs) of one or more Network
                  Load Balancers for your service.
                items:
                  type: string
                type: array
              privateDNSName:
                description: (Interface endpoint configuration) The private DNS name
                  to assign to the VPC endpoint service.
                type: string
              tagSpecifications:
                description: The tags to associate with the service.
                items:
                  description: The tags to apply to a resource when the resource is
                    being created.
                  properties:
                    resourceType:
                      type: string
                    tags:
                      items:
                        description: Describes a tag.
                        properties:
                          key:
                            type: string
                          value:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
            type: object
          status:
            description: VPCEndpointServiceConfigurationStatus defines the observed
              state of VPCEndpointServiceConfiguration
            properties:
              ackResourceMetadata:
                description: All CRs managed by ACK have a common `Status.ACKResourceMetadata`
                  member that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: 'ARN is the Amazon Resource Name for the resource.
                      This is a globally-unique identifier and is set only by the
                      ACK service controller once the controller has orchestrated
                      the creation of the resource OR when it has verified that an
                      "adopted" resource (a resource where the ARN annotation was
                      set by the Kubernetes user on the CR) exists and matches the
                      supplied CR''s Spec field values. TODO(vijat@): Find a better
                      strategy for resources that do not have ARN in CreateOutputResponse
                      https://github.com/aws/aws-controllers-k8s/issues/270'
                    type: string
                  ownerAccountID:
                    description: OwnerAccountID is the AWS Account ID of the account
                      that owns the backend AWS service API resource.
                    type: string
                required:
                - ownerAccountID
                type: object
              conditions:
                description: All CRS managed by ACK have a common `Status.Conditions`
                  member that contains a collection of `ackv1alpha1.Condition` objects
                  that describe the various terminal states of the CR and its backend
                  AWS service API resource
                items:
                  description: Condition is the common struct used by all CRDs managed
                    by ACK service controllers to indicate terminal states  of the
                    CR and its backend AWS service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              serviceConfiguration:
                description: Information about the service configuration.
                properties:
                  acceptanceRequired:
                    type: boolean
                  availabilityZones:
                    items:
                      type: string
                    type: array
                  baseEndpointDNSNames:
                    items:
                      type: string
                    type: array
                  gatewayLoadBalancerARNs:
                    items:
                      type: string
                    type: array
                  managesVPCEndpoints:
                    type: boolean
                  networkLoadBalancerARNs:
                    items:
                      type: string
                    type: array
                  privateDNSName:
                    type: string
                  privateDNSNameConfiguration:
                    description: Information about the private DNS name for the service
                      endpoint. For more information about these parameters, see VPC
                      Endpoint Service Private DNS Name Verification (https://docs.aws.amazon.com/vpc/latest/userguide/ndpoint-services-dns-validation.html)
                      in the Amazon Virtual Private Cloud User Guide.
                    properties:
                      name:
                        type: string
                      state:
                        type: string
                      type_:
                        type: string
                      value:
                        type: string
                    type: object
                  serviceID:
                    type: string
                  serviceName:
                    type: string
                  serviceState:
                    type: string
                  serviceType:
                    items:
                      description: Describes the type of service for a VPC endpoint.
                      properties:
                        serviceType:
                          type: string
                      type: object
                    type: array
                  tags:
                    items:
                      description: Describes a tag.
                      properties:
                        key:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
